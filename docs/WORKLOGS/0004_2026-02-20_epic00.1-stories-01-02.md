# Worklog: Epic 00.1 — Stories 01 and 02 Complete

**Date:** 2026-02-20
**Session:** Implemented RemediationJob CRD types (STORY_01) and domain interfaces (STORY_02) for epic00.1-interfaces
**Status:** Complete

---

## Objective

Implement STORY_01 (core domain types) and STORY_02 (domain interfaces) of epic00.1.
Produce `api/v1alpha1/remediationjob_types.go`, `internal/domain/provider.go`, and
`internal/domain/interfaces.go` as the foundation for all subsequent controller stories.

---

## Work Completed

### 1. TDD and Implementation (delegated)

Tests written first (remediationjob_types_test.go), confirmed failing before implementation,
then implementation delegated to produce:

- `api/v1alpha1/remediationjob_types.go`: all types from REMEDIATIONJOB_LLD §2 —
  `RemediationJobSpec`, `ResultRef`, `FindingSpec`, `RemediationJobStatus`, `RemediationJob`,
  `RemediationJobList`; phase and condition constants; `SourceTypeK8sGPT`; `AddRemediationToScheme`;
  `NewScheme()` helper; deep copy methods with pointer-level independence.

- `internal/domain/provider.go`: `SourceProvider` interface (4 methods), `Finding`, `SourceRef`,
  `SinkConfig` per PROVIDER_LLD §3-4.

- `internal/domain/interfaces.go`: `JobBuilder` interface.

### 2. Code Review (delegated) — 11 gaps found

- GAP 2 (Critical): `AddToScheme` was bundling both group registrations — explicitly forbidden by STORY_01. Fixed by splitting into `AddRemediationToScheme` (only `remediation.mendabot.io`) and keeping `AddResultToScheme` separate.
- GAP 3: `NewScheme()` and `NewResultScheme()` were silently discarding errors — fixed to panic.
- GAP 4: Missing `+kubebuilder:validation:Required` marker on `SourceResultRef` — added.
- GAP 5: `SinkConfig` struct missing from `domain/provider.go` — added.
- GAPs 6–11: Test quality issues — vacuous tests removed, pointer-mutation tests replaced with value-mutation tests, duplicates removed.

### 3. Second review pass — 3 new gaps found

- GAP A: `TestRemediationJob_DeepCopyInto_FindingIndependent` was vacuous (string fields) — removed.
- GAP B: DispatchedAt/CompletedAt tests used pointer reassignment instead of value mutation — fixed to `*dst.Status.X = later`.
- GAP C: Residual smoke tests in provider_test.go — removed.

### 4. Third review pass — APPROVED

Zero gaps remaining. All tests passing with `-race`.

---

## Key Decisions

| Decision | Rationale |
|---|---|
| `AddRemediationToScheme` separate from `AddResultToScheme` | STORY_01 explicitly forbids bundling — prevents duplicate registration errors if both are called from different code paths |
| `SinkConfig.AdditionalEnv map[string]string` | PROVIDER_LLD §4 defines it this way; map[string]string is the correct type for runtime env-var passthrough |
| Value-mutation tests for pointer fields | Pointer reassignment tests never catch shallow-copy bugs; only dereferenced mutation proves independence |

---

## Blockers

None.

---

## Tests Run

```
go test -timeout 30s -race ./api/v1alpha1/... ./internal/domain/...
go build ./...
go vet ./...
```
All clean.

---

## Next Steps

Begin epic00.1 STORY_03 — provider and reconciler skeletons + full `cmd/watcher/main.go` wiring:

1. Create `internal/provider/k8sgpt/provider.go` — `K8sGPTProvider` stub implementing `domain.SourceProvider` with `panic("not implemented")` methods and compile-time assertion.
2. Create `internal/provider/k8sgpt/reconciler.go` — empty file (will hold `fingerprintFor()` in epic01).
3. Create `internal/provider/provider.go` — `SourceProviderReconciler` struct with stub `Reconcile` and `SetupWithManager`.
4. Create `internal/controller/remediationjob_controller.go` — `RemediationJobReconciler` struct with stub methods.
5. Create `internal/jobbuilder/job.go` — `Builder` struct with stub `Build()` and compile-time assertion.
6. Rewrite `cmd/watcher/main.go` — full manager wiring per CONTROLLER_LLD §7.
7. Verify `go build ./...` compiles with stubs.

---

## Files Modified

| File | Action |
|---|---|
| `api/v1alpha1/remediationjob_types.go` | Created |
| `api/v1alpha1/remediationjob_types_test.go` | Created |
| `api/v1alpha1/result_types.go` | Updated (panic on scheme error) |
| `internal/domain/provider.go` | Created |
| `internal/domain/provider_test.go` | Created |
| `internal/domain/interfaces.go` | Created |
| `internal/domain/interfaces_test.go` | Created |
| `docs/BACKLOG/epic00.1-interfaces/STORY_01_domain_types.md` | Updated (Complete) |
| `docs/BACKLOG/epic00.1-interfaces/STORY_02_builder_interface.md` | Updated (Complete) |
| `go.mod` | Updated (go mod tidy) |
| `go.sum` | Updated (go mod tidy) |
