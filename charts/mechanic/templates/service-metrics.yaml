{{- if .Values.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "mechanic.fullname" . }}-metrics
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mechanic.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "mechanic.selectorLabels" . | nindent 4 }}
  ports:
  - name: metrics
    port: 8080
    targetPort: 8080
    protocol: TCP
{{- end }}
