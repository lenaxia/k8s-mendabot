{{- if and .Values.rbac.create (eq .Values.watcher.agentRBACScope "namespace") }}
---
# Grants the namespace-scoped agent ServiceAccount get/list/watch access within the
# mechanic namespace itself, so agent Jobs can read RemediationJob objects.
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "mechanic.fullname" . }}-agent-ns-mechanic
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mechanic.labels" . | nindent 4 }}
subjects:
- kind: ServiceAccount
  name: {{ include "mechanic.agentNSSAName" . }}
  namespace: {{ .Release.Namespace }}
roleRef:
  kind: ClusterRole
  name: {{ include "mechanic.fullname" . }}-agent
  apiGroup: rbac.authorization.k8s.io
{{- end }}
