{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "agentType": {
      "type": "string",
      "enum": ["opencode", "claude"],
      "description": "Agent runner type. Controls which AI agent binary runs inside the Job."
    },
    "image": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "repository": { "type": "string" },
        "tag": { "type": "string" },
        "pullPolicy": {
          "type": "string",
          "enum": ["Always", "IfNotPresent", "Never"]
        }
      }
    },
    "agent": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "image": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "repository": { "type": "string" },
            "tag": { "type": "string" }
          }
        }
      }
    },
    "gitops": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "repo": { "type": "string" },
        "manifestRoot": { "type": "string" }
      }
    },
    "watcher": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "stabilisationWindowSeconds": { "type": "integer", "minimum": 0 },
        "maxConcurrentJobs": { "type": "integer", "minimum": 1 },
        "remediationJobTTLSeconds": { "type": "integer", "minimum": 1 },
        "sinkType": { "type": "string" },
        "logLevel": {
          "type": "string",
          "enum": ["debug", "info", "warn", "error"]
        },
        "llmProvider": {
          "type": "string",
          "enum": ["", "openai"],
          "description": "LLM readiness gate. 'openai' enables it; empty disables."
        },
        "injectionDetectionAction": {
          "type": "string",
          "enum": ["log", "suppress"]
        },
        "agentRBACScope": {
          "type": "string",
          "enum": ["cluster", "namespace"]
        },
        "agentWatchNamespaces": {
          "type": "string",
          "description": "Comma-separated namespaces. Required when agentRBACScope=namespace."
        },
        "watchNamespaces": {
          "type": "string",
          "description": "Comma-separated namespaces the watcher monitors. Empty = all namespaces."
        },
        "excludeNamespaces": {
          "type": "string",
          "description": "Comma-separated namespaces the watcher ignores. Empty = no exclusions."
        },
        "maxInvestigationRetries": {
          "type": "string",
          "description": "Maximum investigation retries per RemediationJob before permanently failing."
        },
        "dryRun": {
          "type": "boolean",
          "description": "Enable dry-run shadow mode. Blocks write operations; stores investigation reports in RemediationJob.status.message."
        },
        "disableCorrelation": {
          "type": "boolean",
          "description": "Disable Epic 13 multi-signal correlation. Default is false (correlation enabled)."
        },
        "correlationWindowSeconds": {
          "type": "integer",
          "minimum": 0,
          "description": "Time window for correlation detection. Findings within this window are considered related."
        },
        "multiPodThreshold": {
          "type": "integer",
          "minimum": 1,
          "description": "Minimum number of pods failing on the same node to trigger MultiPodSameNodeRule."
        },
        "prAutoClose": {
          "type": "boolean",
          "description": "Automatically close open GitHub PRs/issues when the underlying finding resolves."
        }
      }
    },
    "prompt": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "coreOverride": { "type": "string" },
        "agentOverride": { "type": "string" }
      }
    },
    "networkPolicy": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": { "type": "boolean" },
        "apiServerPort": { "type": "integer", "minimum": 1, "maximum": 65535 },
        "additionalEgressRules": { "type": "array" }
      }
    },
    "rbac": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "create": { "type": "boolean" }
      }
    },
    "secrets": {
      "type": "object",
      "description": "Placeholder — no configurable fields; secret names are derived from agentType in job.go"
    },
    "createNamespace": { "type": "boolean" },
    "selfRemediation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "maxDepth": {
          "type": "integer",
          "minimum": 0,
          "maximum": 10,
          "description": "Maximum self-remediation chain depth. 0 disables self-remediation."
        },
        "cooldownSeconds": {
          "type": "integer",
          "minimum": 0,
          "maximum": 3600,
          "description": "Minimum time between allowed self-remediations in seconds. 0 disables circuit breaker."
        },
        "upstreamRepo": {
          "type": "string",
          "description": "Upstream repository for contributions (legacy, unused)."
        },
        "disableUpstreamContributions": {
          "type": "boolean",
          "description": "Disable upstream contributions (legacy, unused)."
        },
        "disableCascadeCheck": {
          "type": "boolean",
          "description": "Disable cascade check."
        },
        "cascadeNamespaceThreshold": {
          "type": "integer",
          "minimum": 0,
          "description": "Cascade namespace threshold."
        },
        "cascadeNodeCacheTTLSeconds": {
          "type": "integer",
          "minimum": 0,
          "description": "Cascade node cache TTL in seconds."
        }
      }
    },
    "metrics": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": { "type": "boolean" },
        "serviceMonitor": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "enabled": { "type": "boolean" },
            "interval": { "type": "string" },
            "scrapeTimeout": { "type": "string" },
            "labels": { "type": "object" }
          }
        }
      }
    }
  }
}
